export enum PriorityEnum {
  LOW = "Low",
  MEDIUM = "Medium",
  HIGH = "High",
}

export enum StatusEnum {
  TODO = "todo",
  IN_PROGRESS = "in_progress",
  DONE = "done",
}

export enum SnackbarTypeEnum {
  SUCCESS = "success",
  ERROR = "error",
}
